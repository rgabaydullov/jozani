import styled from 'styled-components';

export const StyledSectionPromo = styled.section`
  display: flex;
  flex-direction: column;
  justify-content: center;
  margin: 6.4rem 0;
  padding: 2.4rem 0;
  background: linear-gradient(135deg, #A5C0EE 0%, #FBC5EC 100%);
`;
